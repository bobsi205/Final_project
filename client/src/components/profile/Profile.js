import React from 'react';
import PropTypes from 'prop-types';

const Profile = (props) => {
  return (
    <div>
      <p>this is profile</p>
    </div>
  );
};

Profile.propTypes = {};

export default Profile;
